# 更新日志

## v1.1.3 (2026-09-11)

### 修复

- **漏导入 `ImageFilter`（清晰度选帧静默失效）**：v1.1.2 的分段清晰度抽帧使用
  `ImageFilter.FIND_EDGES` 但未导入，NameError 被 `_frame_sharpness` 的异常兜底
  吞掉，每帧清晰度恒为 0，选帧实际退化为"每段取首个候选帧"——时序覆盖与防重
  逻辑不受影响，但"避开运动模糊帧"的目标失效，且日志无任何提示。
  已补导入；self_test 新增两条回归断言（清晰度计算可用、选帧结果真实受清晰度影响）。
- 模块 docstring 更新为现行架构（替换+落库前恢复为主流程，ghost 仅用于表情
  bypass），移除对旧 bypass 主流程的过时描述。

## v1.1.2 (2026-09-11)

### 新增

- **自适应帧数**（新配置 `merge.adaptive_frames`，默认开）：粗抽 ≤24 个样本帧，
  以相邻帧感知哈希的平均距离衡量运动量——几乎静止/循环重复的简单动画自动
  取最少帧数（省 token），运动丰富、帧数多的动画自动取最多帧数（保证证据），
  在『最少/最多帧数』间线性浮动；调节结果写入日志（"自适应帧数: N 帧"）。
  关闭后始终抽『最多帧数』。
- **分段清晰度抽帧**：首尾帧固定保留，中间时间轴均分为 count-2 段，每段在
  候选帧中选取边缘强度（清晰度）最高的一帧。等间隔抽帧容易命中快速运动的
  模糊中间帧，视觉模型对模糊帧的描述常与实际内容偏差很大；新策略既保持
  时序覆盖，又倾向落在姿态明确的瞬间。实际对比：同一柴犬 GIF 旧版第 3/5 格
  为糊掉的转头中间帧，新版各帧表情与动作均清晰可辨。
- **段内差异防重（dhash）**：段内最清晰帧与上一选中帧几乎相同时（感知哈希
  汉明距离 < 6），改选段内与上一帧差异最大的一帧——"新元素入画"（如人手
  出现、姿态突变、被外力拿起等）不再依赖清晰度碰运气，稳定入选。
- **说明条加入连播提示**：文案调整为"动图分镜 · 共 N 帧（按序号连播）"，
  引导视觉模型把跨帧动作（手入画 → 接触）连成连续因果，而不是把每格当作
  独立画面，减少"被扇巴掌被读成被挠痒"这类动作主体/方向误读。

### 修复

- **描述搬运任务从未执行（关键）**：v1.1.1 的 `after_process` 外层循环先
  `pop` 了搬运映射，`_spawn_relocate_task` 启动的搬运任务内部再次 `pop` 同一
  key 拿到空值后直接退出——多帧描述永远无法写回原图记录，原图记录停留在
  "识别中"状态：planner 反复等待（"仍有 1 张图片正在等待识别"）、刷新器
  无描述可回填。改为显式传参（合成图 hash / 类型 / 原图 hash），登记映射由
  `after_process` 循环统一消费。
- 恢复匹配增加 hash 校验：组件列表若被其他环节改动导致索引错位，放弃恢复
  并告警（保持合成图状态），避免把原图字节写错到别的组件上。
- 搬运任务与自检的测试桩签名同步更新。

## v1.1.1 (2026-09-11)

### 修复

- **首次发送的 GIF 以九宫格落库（关键）**：v1.1.0 给替换组件打的内部标记
  `_gs_kind` 在宿主钩子之间的序列化往返中被丢弃（宿主重建组件对象只认已知
  字段），`after_process` 因此认不出该恢复的组件，恢复从未发生，消息以合成图
  形态落库——聊天记录/WebUI 显示九宫格，只有识别缓存过的 GIF 才显示正常。
  改为**索引登记制**：`before_process` 记录"组件索引 → (合成图 hash, 原图
  hash)"，`after_process` 按索引精确恢复；恢复信息完全脱离组件对象。
- **说明条文字截断**：小图合成时固定字号的长文案超出画布宽度被截断。
  文案精简为"动图分镜 · 共 N 帧"，字号按画布宽度自适应（18px 起逐级缩小到
  12px），任何尺寸都完整显示。
- 长文案反而干扰识别的问题一并解决：视觉模型只需"动图分镜"这个锚点即可
  把网格图理解为动画，无需说教式长句。

### 变更

- 替换登记与表情 ghost 登记拆分为独立结构（`_replaced_index` /
  `_emoji_ghosts`），同一消息里同 hash 的图片+表情组件可正确共存处理。
- **帧序号清晰化**：序号字体改用系统矢量字体（原 Pillow 内置字体在旧版
  会退化为小位图字体放大后发虚），并加白色描边——黑字白边，暗色/浅色
  背景上都清晰可读。

## v1.1.0 (2026-09-11)

### 变更（架构重构：ghost → 替换+落库前恢复）

- **普通图片 GIF 改为"识别窗口内替换 + 落库前恢复"**：`before_process` 把组件
  二进制与 hash 暂时替换为合成图（原图字节暂存内存缓存），宿主 `process()`
  识别的就是多帧网格图——多帧描述**直接**成为该图的第一份描述；`after_process`
  在消息落库**之前**用暂存的原图字节恢复组件，原图文件与图片记录按原图 hash
  全部保真。
- 相比 ghost 模式的收益：
  - **根除"首帧描述固化"问题**：原图记录由落库流程创建且从不参与宿主识别，
    不存在首帧描述，刷新器在搬运完成后回填的必然是多帧描述，本轮规划
    （等待上限内）即可生效；
  - **识别次数 2→1**：不再对原图 GIF 做首帧识别，省一半 VLM 调用；
  - **不再需要上下文注入**：移除 v1.0.6 的 `maisaka.context.append` 通道与
    `merge.context_append` 配置（注入的"凭空消息"会干扰 REPLY 模型）；
  - 组件无 ghost，不再有追加组件的 token 与展示负担。
- 搬运任务简化：不再等待原图识别（原图不参与识别），只需等落库建好原图记录；
  超时（默认 10 分钟）放弃并告警。
- 组件携带内部标记 `_gs_kind` 区分"替换件/表情 ghost"，`after_process` 按标记
  分别恢复或回收（同一消息里同 hash 的图片+表情可共存），恢复时移除标记。
- 新增 `merge.clean_ghost_components` 语义扩展：同时负责"落库前恢复原图"与
  "回收表情旁路 ghost"（配置键名不变，避免用户配置迁移）。

### 已知边界

- 若 after_process 阶段异常（极低概率），消息会以合成图形态落库（原图丢失），
  日志会有 warning；`before_process`/`after_process` 之间的崩溃同样如此。
- 表情包 bypass 仍为 ghost 模式：多帧描述写入 EMOJI 记录，运行中的会话无法
  利用（表情组件文本被同步识别固化），宿主重启重建历史后生效。

## v1.0.6 (2026-09-11)

### 修复

- **放弃 `no_file_flag` 压制，改用确定性的上下文注入通道**：实测（v1.0.5 日志）
  压制明明成功写入，3 秒后刷新器仍回填了首帧描述——宿主内有多处代码会按
  "文件系统真相"恢复该标记（消息落库时的图片记录同步 `utils_message`、图片缓存
  定期清理 `image_cache_cleanup`、表情库回填等），针对它的压制必然被击穿，
  该方案整体作废，相关代码全部移除。
- **新增：分镜摘要注入会话上下文**（新能力 `maisaka.context.append`，已声明）。
  多帧描述搬运新写入成功后，插件把 `[GIF动画分镜] …` 作为一条上下文消息追加进
  当前会话（stream_id 直接取消息自带的 session_id）——不依赖刷新器回填，
  **下一轮规划麦麦必然看到完整分镜**。按 stream_id+内容去重，每张 GIF 每会话
  只注入一次；新增配置项 `merge.context_append`（默认开）。
- 搬运写库仍然保留：宿主重启后聊天历史重建，刷新器会从原图 hash 读到多帧描述，
  两条通道互为补充。

### 变更

- `_process_component` 增加 `stream_id` 参数；ghost 登记携带会话流 ID，
  供搬运完成后注入上下文使用。

## v1.0.5 (2026-09-11)

### 修复

- **与消息防抖类插件共存（重要）**：`chat.receive.before_process` 钩子从 `EARLY`
  改为 `LATE` 槽位。防抖插件（如 Message_Debouncing_Refactored）在 `NORMAL` 槽
  阻塞窗口并合并消息——原先本插件排在它**之前**，防抖窗口内被并入的第 2..N 条
  消息虽然经过本插件（登记了 ghost），但整条消息随后被防抖 abort 吞掉：那些 GIF
  永远不会被分镜处理，且留下永远不会被回收的 ghost 登记。改为 `LATE` 后本插件
  处理的是防抖放行后的最终合并消息（全部 GIF 均被分镜），被 abort 的消息也不会
  再触发本插件。防抖超时（ErrorPolicy.SKIP）时消息仍会正常到达本插件，不会丢失。
- **压制可观测性**：`no_file_flag` 压制此前失败/未匹配记录时仅输出 debug 日志，
  实际部署中难以发现（曾导致"日志显示搬运成功、planner 却仍是首帧描述"时无从
  判断压制是否执行）。现在压制成功输出 info、失败/匹配 0 行输出 warning 并如实
  标记（退化为宿主原生回填时序）。

### 说明

- 防抖插件本身不会破坏压制时序：它只延迟消息进入处理链（不重新触发
  `before_process`），压制仍发生在其后的 `after_process` 阶段。

## v1.0.4 (2026-09-11)

### 修复

- **重发"已识别过"的 GIF 时上下文仍固化旧首帧描述（实测）**：首帧压制只作用于
  宿主刷新器的回填路径；若该 GIF 此前出现过，`process` 阶段 `get_image_description`
  会缓存命中直接返回旧的首帧描述，组件文本在处理阶段即被固化，压制与搬运均无法
  覆盖（表现为：搬运成功日志后，planner 里仍是首帧描述）。
- 新增**已就绪描述快速路径**：搬运完成后的合成图记录（merged_hash，IMAGE 类型）
  本身就是持久化的多帧描述，且同图重发时帧抽样与 JPEG 编码确定、merged_hash 一致。
  `before_process` 时若合成图记录已完成识别、且原图/原表情记录在库
  （`no_file_flag=False`），直接把多帧描述写入组件 `data`（content）——
  宿主对 content 非空的组件跳过识别，planner 上下文**立即**拿到多帧描述：
  - 零额外 VLM 调用、无 ghost、无搬运、无压制窗口；
  - 普通图片与表情包 bypass 均生效（表情写 `[表情包: …]`、图片写 `[图片：…]`，
    与宿主原生格式一致）；
  - 未命中（首次出现/图片文件缺失/参数指纹变化）自动退回 ghost + 压制 + 搬运流程。

## v1.0.3 (2026-09-11)

### 修复

- **多帧描述被"首帧描述固化"卡在上下文之外（关键）**：实测发现搬运成功后
  planner 上下文仍是 GIF 首帧描述。根因是宿主视觉占位刷新器只把数据库描述
  回填给"空占位"组件——原图（GIF 首帧）识别几乎总比合成图先完成，刷新器会先
  把首帧描述固化进组件文本；此后即使多帧描述已写回原图 hash，刷新器对非占位
  组件不再理会。**纯插件修复，不改宿主任何代码**：
  - `chat.receive.after_process` 回收 ghost 时，把原图 Images 记录临时标记
    `no_file_flag=True`，让首帧描述对刷新器"隐身"（组件保持空占位，pending
    等待判断同时跳过、不拖慢 planner）；
  - 搬运成功时在同一次更新中写入多帧描述并恢复 `no_file_flag=False`，刷新器
    下一轮即把多帧描述回填进麦麦上下文；
  - 搬运超时/异常/插件卸载时同样恢复，避免残留；同 hash 图再次出现时宿主
    `ensure_image_saved` 也会自愈恢复该标记。
  - 已知取舍：压制窗口内（通常几秒，上限 10 分钟）宿主按原图 hash 取文件会
    失败（如回复引用原图）；若同一张 GIF 在搬运窗口内被再次发送，其组件文本
    仍会固化首帧描述（缓存命中路径不检查该标记）。
- 表情包旁路（bypass）的多帧描述仅在宿主表情识别失败/未配置 VLM（组件保持
  `[表情包]` 占位）时能回填进上下文；表情识别成功时组件文本在处理阶段即被
  同步固化，纯插件侧无法覆盖——文档如实说明。

## v1.0.2 (2026-09-11)

### 修复

- **描述搬运彻底失效（关键）**：`_manifest.json` 的 `capabilities` 补上
  `database.query`。v1.0.1 引入描述搬运后开始调用 `ctx.db`，但 manifest 未声明
  对应能力，宿主 `AuthorizationManager` 只对声明过的能力签发令牌——未声明时所有
  `database.query` 调用都被拒绝，多帧描述永远无法写回原图 hash 的 Images 记录，
  麦麦上下文里始终是宿主原生识别结果（GIF 首帧的静态描述），插件看起来"没有效果"。
- 新增 `on_load` 数据库能力自检：主动探测一次 `database.query`，能力缺失时直接输出
  warning 并给出修复提示，不再等到搬运超时才暴露问题。
- `_db_get_image_record` 查询失败由 debug 提升为 warning（带一次性去重），
  便于排查权限/连接问题。

### 改进

- 描述搬运超时新增兜底：若合成图描述已就绪、而原图识别迟迟未完成（如视觉模型
  不支持 image/gif 的供应商），直接把多帧描述写入原图记录，不再白白浪费已完成的
  合成图识别结果；原图识别随后若完成并写入首帧描述会自然覆盖（概率极低）。

## v1.0.1 (2026-09-11)

### 规范（对照 SDK 与宿主源码核查）

- 日志统一改用宿主注入的 `ctx.logger`（自动路由到 `plugin.<plugin_id>`），
  移除游离于宿主日志体系外的自定义 logger；
- `chat.receive.after_process` 回收钩子改用 `HookOrder.LATE` 槽位，
  避免干扰其他插件的 after 阶段处理；
- `host_application.min_version` 提升至 1.2.4：`after_process` 钩点与
  `ctx.db` update 能力在更早版本未经验证，宿主不支持时插件会注册失败，
  必须如实声明兼容下限。

### 修复

- **视觉描述"九宫格"化**：合成图顶部新增"动画分镜"说明条（`storyboard_caption`，默认开启），
  明确引导视觉模型把网格图理解为同一段动画的时序帧，而不是"一张九宫格动漫截图"；
  环境中无中文字体时自动改用英文说明。
- **合成图污染聊天记录**：普通图片 GIF 从"原位替换"改为与表情包一致的 ghost 模式——
  原图/原表情组件一律原样保留（图片库/表情库/WebUI 全部保真），合成图只在宿主
  `process()` 识别阶段存在；新增 `chat.receive.after_process` 钩子在识别调度完成后
  回收全部 ghost 组件（`clean_ghost_components`，默认开启），九宫格不再随消息落库展示。

### 新增

- 描述搬运机制：ghost 回收后由后台任务轮询宿主识别结果（最长 10 分钟），把多帧动画
  描述写入原图 hash 的 Images 记录，宿主视觉占位刷新器自动将其回填进麦麦上下文；
  超时放弃时行为退化为宿主原生识别（原图/首帧描述），绝不阻塞消息链。

## v1.0.0 (2026-09-11)

- 首个版本：GIF 动图分镜（视觉增强），插件 id `github.elmeir.gif-storyboard`。
- 只处理 GIF：仅对 `GIF87a` / `GIF89a` 魔数的多帧动图替换，PNG/JPEG/WebP 等其他格式零接触，单帧 GIF 视为静态图放行。
- 挂载宿主 `chat.receive.before_process` 钩子（BLOCKING / EARLY），把消息里 `image` 组件中的 GIF 动图
  原位替换为多帧合成网格图。
- **表情包组件三策略**（`emoji_strategy`，默认 `bypass` 旁路识别）：宿主表情链路"识别与存储共用同一个
  字节流"——直接替换会让 `emoji_manager` 把合成图落盘 `data/emoji`（Images 表 EMOJI 类型）甚至被收藏
  发出。`bypass`（默认）：原表情组件原样保留（表情库存原始 GIF，收藏/发送保真），另追加一个 content 为空
  的 image 组件携带合成网格图，视觉链路借道图片描述完成多帧识别，麦麦上下文同时出现 [表情包] 与
  [图片: 多帧描述]；`replace`：直接替换（识别最直接，代价是表情库污染）；`off`：不处理。
- 帧抽取：按时序均匀抽样（含首尾帧），`max_frames` 控制上限（默认 9 = 3x3 网格）；
  单帧 GIF 视为静态图直接放行（`min_frames` 可调）。
- 合成图：网格排布尽量接近正方形，帧间隔 `grid_gap`（白底）、单帧格子边长上限
  `max_cell_size`；每帧左上角可绘制序号（`draw_frame_index`）帮助视觉模型理解播放顺序；
  输出 jpeg（默认，体积小）/ png 可选。
- 替换后同步写回新字节的 SHA-256 hash；宿主 `ByteComponent` 在二进制非空时本就会
  重算 hash，双保险保持字典自洽，后续识别与落库自动使用合成图。
- 合成结果按「原图哈希 + 参数指纹」做内存 LRU 缓存（上限 64 条），重复出现的
  同一 GIF 不再重复解码；配置热更新时自动清空。
- 失败兜底：解码失败 / 合成异常 / 单帧 GIF 一律原样放行，绝不阻塞消息链
  （`ErrorPolicy.SKIP`，钩子超时 10s）。
- 不改动组件 `data`（content）字段——宿主以 content 非空判定"已识别过"，
  乱填会导致图片跳过视觉模型识别。
